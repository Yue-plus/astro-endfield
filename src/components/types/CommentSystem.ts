export type Valine = {
  appId: string;
  appKey: string;
  serverUrl?: string;
};

export type Gitalk = {
  clientId: string;
  clientSecret: string;
  repo: string;
  owner: string;
  admin: string[];
  id: string;
};

export type Waline = {
  serverUrl: string;
};
